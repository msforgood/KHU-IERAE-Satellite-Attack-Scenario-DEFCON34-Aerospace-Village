# 커밋 (출력만, 실제 실행 금지)
- 작업 후 변경 파일별 `git add` + `git commit` 명령어를 출력.
- 메시지 포맷: `동사(대문자) 대상: 한글 작업내용`
- 동사: NEW(신기능) / UPDATE(보완) / EDIT(수정) / FIX(버그) / BUILD(빌드)
- claude ai 언급·contributor 등록 금지.
- 다중 파일 add는 `/`로 줄바꿈해도 동작하게 출력.

# DO NOT
git checkout / git add / git commit 실제 실행.